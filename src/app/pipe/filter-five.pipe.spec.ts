import { FilterFivePipe } from './filter-five.pipe';

describe('FilterFivePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFivePipe();
    expect(pipe).toBeTruthy();
  });
});
