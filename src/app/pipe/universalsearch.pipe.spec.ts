import { UniversalsearchPipe } from './universalsearch.pipe';

describe('UniversalsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversalsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
