import { UniversalpipePipe } from './universalpipe.pipe';

describe('UniversalpipePipe', () => {
  it('create an instance', () => {
    const pipe = new UniversalpipePipe();
    expect(pipe).toBeTruthy();
  });
});
